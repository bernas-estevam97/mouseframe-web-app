[pytest]
DJANGO_SETTINGS_MODULE = mouseapp.settings
# -- recommended but optional:
addopts = --no-header -v -s -rA
python_files = test_*.py
filterwarnings =  
    ignore::PendingDeprecationWarning
    ignore::FutureWarning
